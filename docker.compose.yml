services:
  web:
    build:
      context: .
      target: prod
    container_name: express-prod
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
